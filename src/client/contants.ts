export const Dim = {
  spacing: 24,
  headerHeight: 88,
  mobileHeaderHeight: 64,
  maxViewWidth: 1920,
};

export const minBlockHeight = `calc(100vh - ${Dim.headerHeight}px - ${Dim.spacing * 2 - 2}px)`;