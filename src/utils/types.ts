export interface BlogEntry {
  tag: string;
  title: string;
  preview: string;
  url: string;
}

export interface Tweet {
  id: string
  text: string
  name: string
  username: string
}

export interface JobRole {
  area: string
  title: string
  type: string
  url: string
}
